import React from "react";

const Button = () => {
  return (
    <button>
      <span role="img" aria-label="arrow back">
        ⬅ Back
      </span>
    </button>
  );
};

export default Button;
